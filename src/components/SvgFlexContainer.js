import React from 'react';
import { NavLink } from 'react-router-dom';


const SvgFlexContainer = (props) => {
  return (
    <div className="svg-flex-cont">{props.children}</div>
);}

export default SvgFlexContainer;
